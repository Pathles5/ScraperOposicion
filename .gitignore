# Dependencias
node_modules/
.pnp.*

# Build
dist/
build/

# Env
.env
.env.local

# Coverage
coverage/

# Sesion (journal NO se commitea)
SESSION_CONTEXT.md

# OpenCode runtime (si existe)
.opencode/node_modules/

# Estado runtime del monitor (Fase 3)
state/*.fingerprint
state/*.fingerprint.tmp
state/.initialized
logs/*.log
scripts/raspberry/telegram.env
